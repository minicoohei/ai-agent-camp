---
description: Cuando el usuario dice /module-24-exercise - checklist de ejercicios del Module 24 (SOQL / CSV / metadatos)
nonInteractiveMode: incompatible
---
## Empieza Aqui (ruta mas corta)

Cuando ejecutes **`/module-24-exercise`** en el chat, puedes avanzar por las 8 tareas del ejercicio de module-24 en orden.

# Module 24 - Checklist de ejercicios

## Tareas (en orden)

1. `sf --version` / login en Sandbox
2. `sf config set target-org=dev`
3. SOQL (equivalente a `/module-24-soql`)
4. CSV insert de 5 registros
5. ExternalId upsert
6. retrieve (`/module-24-metadata-retrieve`)
7. Agregar una clase Apex de ejemplo
8. deploy (`/module-24-metadata-deploy`)

Resume el resultado de cada paso. Si algo falla, consulta `slideId=pitfalls`.

## Referencias

- Curso: `slideId=exercise` (ejemplo: `/es/course/module-24?slideId=exercise`)
