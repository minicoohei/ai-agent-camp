---
description: When the user says /module-24-exercise - Module 24 exercise checklist (SOQL / CSV / metadata)
nonInteractiveMode: incompatible
---
## Start Here (Shortest Path)

When you run **`/module-24-exercise`** in chat, you can work through the 8 module-24 exercise tasks in order.

# Module 24 - Exercise Checklist

## Tasks (in order)

1. `sf --version` / Sandbox login
2. `sf config set target-org=dev`
3. SOQL (equivalent to `/module-24-soql`)
4. CSV insert, 5 records
5. ExternalId upsert
6. retrieve (`/module-24-metadata-retrieve`)
7. Add a sample Apex class
8. deploy (`/module-24-metadata-deploy`)

Summarize the result of each step. If something fails, refer to `slideId=pitfalls`.

## References

- Course: `slideId=exercise` (e.g., `/en/course/module-24?slideId=exercise`)
