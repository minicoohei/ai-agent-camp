---
description: When the user says /module-24-exercise — Module 24 演習チェックリスト（SOQL / CSV / メタデータ）
nonInteractiveMode: incompatible
---
## まずこれ（最短）

チャットで **`/module-24-exercise`** を実行すると、module-24 演習 8 タスクを順に進められます。

# Module 24 — 演習チェックリスト

## タスク（順番）

1. `sf --version` / Sandbox ログイン
2. `sf config set target-org=dev`
3. SOQL（`/module-24-soql` 相当）
4. CSV insert 5 件
5. ExternalId upsert
6. retrieve（`/module-24-metadata-retrieve`）
7. サンプル Apex 追加
8. deploy（`/module-24-metadata-deploy`）

各ステップの結果を要約。失敗時は `slideId=pitfalls` を参照。

## 参照

- コース: `slideId=exercise`（例: `/ja/course/module-24?slideId=exercise`）
