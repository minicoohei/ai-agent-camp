#!/usr/bin/env bash
# generate-rules.sh — (廃止) rules/ は .cursor/rules/ に統合済み
#
# .cursor/rules/ が正本。rules/ ディレクトリは廃止。
# このスクリプトは後方互換性のため残しているが、何もしない。

echo "INFO: rules/ は .cursor/rules/ に統合されました。"
echo "INFO: .cursor/rules/ を直接編集してください。"
