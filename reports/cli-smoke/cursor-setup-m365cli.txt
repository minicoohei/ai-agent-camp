`.cursor/commands/lesson/setup-m365cli.md` の YAML には **`nonInteractiveMode: deferred`** とあります（`incompatible` ではありません）。

**このモードでの扱い（実行はしない前提）**

- ブラウザで URL を開き、デバイスコード入力が必要なため、ノンインタラクティブ／`claude -p` などでは **`m365 login` まで自動で進めない** と理解する。
- レッスン本文どおりなら **`setup-resume.md` を用意して読み取り系の確認のみ**し、その後ユーザーが対話環境または手順どおりログインすることを続行。
- **やることの要約**: Node 18+ 確認、`@pnp/cli-microsoft365@7.x` のグローバルインストール、`m365 login`（人手）、`m365 status` で接続確認。WSL 時はブラウザをホスト側で開くなどの注意のみ記載。

このチャットでは手順実行・インストール・ログインは行いません。対話環境へ切り替えたときに上記から再開してください。
